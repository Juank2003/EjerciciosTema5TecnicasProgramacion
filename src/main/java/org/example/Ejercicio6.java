package org.example;

public class Ejercicio6 {
/*El algoritmo anterior es una tabla bidimensional de 3x3 con valores del 1 al 9.
Cada valor se asigna a una posición en la tabla usando dos bucles donde se recorre cada fila y columna de la tabla.*/

    /*1.

    Algo QueHace

    #??
    Constante TAMAÑO : entero <- 4
    Variable i, j, val: entero
    Variable tab2d: entero[TAMAÑO][TAMAÑO]
    Inicio
    val <- 1
    Para i <- 0 a TAMAÑO - 1
    Para j <- 0 a TAMAÑO - 1
    tab2d[j][i] <- val
    val <- val + 1
    FPara
    FPara
    Fin*/

    /*2
    Algo QueHace

    #??
    Constante TAMAÑO : entero <- 4
    Variable i, j: entero
    Variable tab2d: entero[TAMAÑO][TAMAÑO]
    Inicio
    Para j <- 0 a TAMAÑO - 1
    Para i <- 0 a TAMAÑO - 1
    tab2d[i][j] <- i + j
    FPara
    FPara
    Fin */


}
